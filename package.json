{
  "name": "revenue-bridge",
  "private": true,
  "version": "1.0.0",
  "description": "Revenue Bridge - B2B pipeline engineering. React/Vite frontend + Node/Express/MongoDB backend.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c yellow,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "mail:test": "npm run mail:test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
